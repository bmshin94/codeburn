# CodeBurn 분석 정리 (한국어)

> 이 문서는 `bmshin94/codeburn` 저장소를 직접 열어보고 분석한 내용을 한국어로 정리한 것입니다.
> 작성일: 2026-09-11 · 분석 기준 버전: `0.9.24`

---

## 🔗 관련 링크

| 구분 | 주소 |
|---|---|
| **내 저장소 (이 레포)** | https://github.com/bmshin94/codeburn |
| **원본 저장소 (upstream)** | https://github.com/getagentseal/codeburn |
| npm 패키지 | https://www.npmjs.com/package/codeburn |
| 이슈 트래커 | https://github.com/getagentseal/codeburn/issues |
| 제작사 | https://agentseal.org (AgentSeal) |
| 가격 데이터 출처 | https://github.com/BerriAI/litellm |
| 환율 데이터 출처 | https://www.frankfurter.app/ |
| Omarchy 플러그인 (커뮤니티) | https://github.com/erzz/omarchy-codeburn |

> 참고: 이 레포의 최신 커밋은 `Add files via upload` 로, 원본을 업로드해온 형태입니다.
> 원본 프로젝트의 `package.json` repository 필드도 `getagentseal/codeburn` 을 가리킵니다.

---

## 목차

1. [CodeBurn이 뭔가요](#1-codeburn이-뭔가요)
2. [폴더 구조](#2-폴더-구조)
3. [설치 및 사용법](#3-설치-및-사용법)
4. [플러그인? 스킬? MCP?](#4-플러그인-스킬-mcp)
5. [API 토큰이 필요한가요](#5-api-토큰이-필요한가요)
6. [왜 깃허브에서 유명한가](#6-왜-깃허브에서-유명한가)
7. [로컬 에이전트 구축에 도움이 되는가](#7-로컬-에이전트-구축에-도움이-되는가)
8. [수익화 아이디어](#8-수익화-아이디어)
9. [React나 PHP로 만들 수 있나](#9-react나-php로-만들-수-있나)
10. [분석 중 발견한 것들](#10-분석-중-발견한-것들)

---

## 1. CodeBurn이 뭔가요

한 줄 요약: **AI 코딩 툴에 쓴 돈을 항목별로 쪼개서 보여주는 "AI 가계부"**

### 문제 상황
카드 명세서에는 "AI 툴: 30만원" 한 줄만 찍힙니다. 그 돈이 **어떤 작업에, 어떤 모델에, 어떤 프로젝트에** 나갔는지는 알 수 없습니다.

### CodeBurn의 해결 방식
AI 툴들이 **이미 내 컴퓨터에 저장해둔 세션 로그 파일**을 읽어서 계산합니다.

```
❌ 프록시 방식 (다른 도구들):  내 요청 → 중계 서버 → AI 회사 API
✅ CodeBurn 방식:              이미 저장된 로그 파일 → 읽고 계산 → 끝
```

### 핵심 특징
- **완전 로컬 실행** — 프록시 없음, API 키 없음, 회원가입 없음
- README 원문: *"The CLI sends nothing."* (CLI는 아무것도 보내지 않음)
- **41개 AI 툴 지원** (Claude Code, Cursor, Codex, Copilot, Gemini, Kimi, Cline, Zed, Warp, Devin 등)
- 가격 데이터는 LiteLLM 공개 가격표를 매일 갱신
- **MIT 라이선스**, 무료, 오픈소스
- Node.js 22.13+ 필요

---

## 2. 폴더 구조

```
codeburn/
├── src/          ⭐ 핵심 두뇌 — Node.js CLI 본체 (TypeScript 178개 파일)
│   ├── cli.ts            진입점 (Commander.js)
│   ├── dashboard.tsx     터미널 대시보드 (React + Ink!)
│   ├── providers/        41개 툴별 로그 파서 (claude.ts, cursor.ts, codex.ts …)
│   ├── optimize.ts       낭비 탐지기 (14종)
│   ├── guard/            예산 초과 방지 훅
│   ├── act/              적용/되돌리기(undo) 기록 시스템
│   ├── mcp/              MCP 서버 (server.ts, redact.ts, tables.ts)
│   ├── quota/            제공사별 잔여 한도 조회
│   ├── sync/             팀 telemetry 전송 (프리뷰)
│   ├── sharing/          기기 간 사용량 합산 (PIN 페어링)
│   └── plugins/          자체 플러그인 소켓 (서명 검증 기반)
│
├── mac/          macOS 메뉴바 앱 (Swift + SwiftUI)
├── windows/      Windows 트레이 앱 (Rust/Tauri + React)
├── gnome/        Linux GNOME Shell 확장 (JavaScript)
├── app/          데스크톱 앱 (Electron)
├── dash/         브라우저 대시보드 프론트엔드 (React + Vite)
│
├── tests/        테스트 239개 파일 (vitest)
├── docs/         아키텍처 · 프로바이더별 · sync 문서
├── scripts/      빌드 / 릴리즈 / 성능측정 스크립트
└── README.md     68KB 분량의 문서
```

### 아키텍처 핵심: "두뇌 1개 + 껍데기 N개"

```
        [ CLI 본체 = 두뇌 ]
                 │
    ┌────────┬───┴────┬─────────┐
    ▼        ▼        ▼         ▼
  터미널    웹앱    메뉴바앱   MCP 서버
```

맥/윈도우/그놈 앱들은 **코드를 공유하지 않습니다.** 전부
`codeburn status --format menubar-json --period <p>` 명령을 호출해서 **JSON만 받아씁니다.**
즉 **출력 계약(output contract)만 공유**하는 구조라서, 어떤 화면에서 보든 **숫자가 항상 같습니다.**

---

## 3. 설치 및 사용법

### 설치

```bash
# A. 설치 없이 1회용 실행 (가장 추천)
npx codeburn

# B. 전역 설치
npm install -g codeburn

# C. macOS
brew install codeburn
```

준비물: **Node.js 22.13 이상** (`node -v` 로 확인)

### 자주 쓰는 명령어

| 명령어 | 설명 |
|---|---|
| `codeburn` | 터미널 대시보드 (기본, 오늘 기준) |
| `codeburn web` | 브라우저 대시보드 (localhost:4747, 차트) |
| `codeburn overview` | 이번 달 요약 (복붙용 텍스트) |
| `codeburn optimize` | 낭비 진단 + A~F 등급 |
| `codeburn optimize --apply` | 진단된 설정 문제 자동 수정 |
| `codeburn act list` | CodeBurn이 변경한 내역 전체 |
| `codeburn act undo --last` | 마지막 변경 되돌리기 |
| `codeburn act report` | 예상 절감액 vs 실제 절감액 검증 |
| `codeburn guard install` | 예산 초과 방지 훅 설치 |
| `codeburn compare` | 모델 성능/효율 비교 |
| `codeburn yield` | git 커밋과 연결해 "돈값 했는지" 분석 |
| `codeburn quota` | 제공사별 남은 한도 조회 |
| `codeburn doctor` | 툴 인식 상태 점검 |
| `codeburn menubar` | 맥 메뉴바 / 윈도우 트레이 앱 설치 |

### 대시보드 키보드 단축키

```
← →   기간 전환 (오늘 / 7일 / 30일 / 이번달 / 6개월 / 전체)
1~6   기간 바로가기
↑ ↓   스크롤 (PgUp/PgDn 페이지 이동, Home/End 양끝)
j k   일자 단위 이동
c     모델 비교 화면
o     낭비 분석 화면
q     종료
```

### 첫 사용 추천 코스

```bash
npx codeburn          # ① 대시보드 구경
codeburn doctor       # ② 내 툴이 다 인식됐는지 확인
codeburn optimize     # ③ 낭비 진단받기
```

---

## 4. 플러그인? 스킬? MCP?

**결론: 셋 다 아닙니다. 본체는 독립 CLI 프로그램이고, MCP 서버로도 "겸직"합니다.**

| 질문 | 답 | 설명 |
|---|---|---|
| Claude Code **플러그인**인가? | ❌ 아니다 | `/plugin install` 로 설치하는 게 아님 |
| Claude **스킬**인가? | ❌ 아니다 | `.claude/skills/` 에 들어가는 것도 아님 |
| **MCP**인가? | 🟡 겸직 | 본체는 CLI. `codeburn mcp` 명령으로 MCP 서버로 변신 |

### MCP로 등록하기

```bash
claude mcp add codeburn -- npx -y codeburn mcp
```

등록하면 AI에게 대화 도중 *"이번 주 토큰 어디에 제일 많이 썼어?"* 라고 물어볼 수 있습니다.

노출되는 MCP 도구는 2개입니다 (`src/mcp/server.ts` 확인):

| 도구 | 반환하는 것 |
|---|---|
| `get_usage` | 사용량/비용 + 툴·모델·프로젝트·작업별 분석 (빠름) |
| `get_savings` | 절감 포인트 심층 분석 (느림) |

**프라이버시:** 프로젝트 이름은 기본적으로 가명 처리됩니다(`src/mcp/redact.ts`).
AI가 `include_project_names: true` 를 명시적으로 요청해야만 실제 이름이 노출됩니다.

### 추가로 존재하는 두 가지 확장 개념

- **훅(Hook)** — `codeburn guard install` 은 Claude Code의 `.claude/settings.json` 에
  **예산 감시 훅**을 설치합니다. 플러그인이 아니라 훅입니다.
  훅은 fail-open 설계라서, 훅이 고장나도 세션을 막지 않습니다.
- **자체 플러그인 소켓** — `~/.config/codeburn/plugins/<name>/codeburn-plugin.json`.
  아직 개발 단계이며 **deny-by-default** 설계입니다. 매니페스트에 선언하지 않은 기능은
  통과하지 못하고, 서명되지 않은 플러그인은 `CODEBURN_PLUGIN_DEV=1` 없이는 로딩이 거부됩니다.

---

## 5. API 토큰이 필요한가요

**결론: 기본 기능은 토큰이 하나도 필요 없습니다.**

인터넷에 물어보는 게 아니라, 내 컴퓨터에 이미 저장된 파일을 읽는 방식이기 때문입니다.

### 예외적으로 인증이 필요한 경우

| 기능 | 필요한 것 | 설명 |
|---|---|---|
| `codeburn quota` | 🔵 기존 로그인 정보 | 새 토큰 X. 이미 로그인된 세션/키체인을 읽음 |
| Vercel AI Gateway 추적 | 🟠 API 키 | `AI_GATEWAY_API_KEY` — 유일하게 진짜 키가 필요 |
| `codeburn devices` | 🔵 PIN 번호 | 내 기기끼리 합산. 같은 로컬 네트워크 안에서만 |
| `codeburn sync` | 🟠 회사 계정 로그인 | 팀 단위 전송(프리뷰). OIDC 브라우저 로그인 |

개인이 혼자 쓴다면 **아무것도 필요 없습니다.**

### 텔레메트리 정리

- **CLI: 아무것도 보내지 않음**
- **데스크톱 앱 / 윈도우 트레이:** 첫 실행 동의 화면에서 선택한 경우에만 익명 리포트 전송.
  EU/EEA/영국/스위스 및 지역 불명은 **기본 꺼짐**, 그 외는 기본 켜짐. 언제든 설정에서 변경 가능.
  식별자는 기기에서 생성한 난수 ID 하나뿐이고, 이벤트에는 시각 없이 날짜만 담깁니다.

---

## 6. 왜 깃허브에서 유명한가

### ① 확실한 페인포인트
"AI 구독료가 너무 나간다"는 개발자 커뮤니티의 현재 최대 관심사인데, 다들 정확한 내역을 모르고 있었습니다.

### ② 로컬 온리 = 신뢰
경쟁 도구 대부분은 프록시 방식이라 내 코드와 프롬프트가 남의 서버를 거칩니다.
CodeBurn은 **"아무것도 보내지 않음"** 을 1순위 세일즈 포인트로 내세워 신뢰를 확보했습니다.

### ③ 41개 툴 = 압도적 커버리지
`src/providers/` 에 실제로 40개 넘는 파서 파일이 있습니다. 사용자가 **"내 툴도 있네?"** 하게 됩니다.

### ④ 4개 화면 전부 지원
터미널 + 웹 + 데스크톱 + 메뉴바/트레이. macOS · Windows · Linux 모두 커버.

### ⑤ "정직함"이라는 차별점
- 숫자마다 **measured(실측)** 인지 **estimated(추정)** 인지 명시
- `codeburn act report` 는 3일 뒤 **"내가 제안한 절약이 실제로 효과 있었는지"** 검증하고,
  **효과가 없었으면 스스로 인정**합니다. `--auto-revert` 로 효과 없던 변경을 되돌립니다.
- 자기 실수를 인정하는 도구는 드물고, 개발자들은 여기에 반응합니다.

### ⑥ 문서와 관리 상태
README 68KB, 테스트 239개 파일, 아키텍처/기여/보안/릴리즈 문서 완비,
Product Hunt 등재, Claude 오픈소스 지원 프로그램 선정.

---

## 7. 로컬 에이전트 구축에 도움이 되는가

**결론: 매우 도움이 됩니다. 두 가지 방향으로 쓸 수 있습니다.**

### A. 도구로 직접 활용

```bash
codeburn report --format json | jq '.overview.cost'
```

내가 만든 에이전트의 **비용 모니터링**에 바로 연결할 수 있습니다.
`codeburn guard` 로 **"에이전트가 폭주하면 자동 정지"** 안전장치도 붙일 수 있습니다.
(밤새 돌리는 에이전트에는 사실상 필수)

### B. 코드 패턴 학습 (이쪽이 더 큼)

| 배울 것 | 위치 | 왜 중요한가 |
|---|---|---|
| MCP 서버 구현 | `src/mcp/server.ts` | zod 입력 검증, 중복 요청 합치기(`inflight`) 까지 포함된 실전 구현 |
| 민감정보 가리기 | `src/mcp/redact.ts` | AI에 데이터 줄 때 프로젝트명 익명화 |
| 훅 시스템 | `src/guard/hooks.ts` | AI 세션 중간에 끼어들어 제어하는 법, fail-open 설계 |
| 플러그인 보안 | `src/plugins/loader.ts`, `manifest.ts` | deny-by-default + 서명 검증. 확장 시스템 설계의 정석 |
| 되돌리기(undo) | `src/act/` | 백업 → 저널 기록 → 복구. **에이전트가 파일을 건드릴 때 필수 패턴** |
| 로그 파싱 | `src/providers/` | 40개 툴의 서로 다른 로그를 하나의 형식으로 통일 |
| 아키텍처 | `docs/architecture.md` | "두뇌 1개 + 껍데기 N개" 설계 |

### 특히 추천하는 3곳

1. **`src/act/`** — 변경 전 백업 + 저널 기록 + 복구. 파일이 바뀌었으면 복구를 거부하는 안전장치까지.
2. **`src/mcp/server.ts`** — MCP 서버가 처음이라면 이걸 보고 따라 하면 됩니다. 짧고 깔끔합니다.
3. **`src/plugins/manifest.ts`** — 주석에 이런 문장이 있습니다:
   > *"매니페스트는 메타데이터가 아니라 **보안 계약**이다"*

---

## 8. 수익화 아이디어

### 🚨 핵심 발견: `sync` 수신 서버가 비어 있다

`codeburn sync` 라는 팀 전송 기능이 있는데, 구조가 이렇습니다:

```
[개발자 PC]                          [수신 서버]
codeburn sync push  ──────────→        없음
   ✅ 완성됨                            ❌ 이 레포에 없음
```

**보내는 쪽(클라이언트)은 완성돼 있습니다.** OIDC 로그인, OS 키체인 토큰 보관,
중복 제거 원장(ledger), 재전송 방지까지 전부 구현돼 있습니다.

**그런데 받는 쪽(서버)은 이 저장소에 없습니다.**
게다가 **"서버를 이렇게 만들면 된다"는 규격서가 `docs/sync/DEVELOPER.md` 에 공개**돼 있습니다.

#### 서버가 구현해야 할 것 (문서에 명시됨)

| 항목 | 내용 |
|---|---|
| 디스커버리 | `GET /.well-known/codeburn-export.json` (issuer, client_id, traces_path, max_batch_size 반환) |
| 수신 | `POST /v1/traces` — **OTLP/HTTP JSON** 형식 |
| 인증 | OIDC Authorization Code + PKCE, 고정 콜백 포트 `19876~19878` |
| 중복 제거 | span_id = SHA-256(deduplicationKey)[:8], trace_id = SHA-256(sessionId)[:16] — 재전송은 바이트 동일 |

#### 서버로 들어오는 데이터 (일부)

```
ai.provider            어떤 툴 (claude / cursor / …)
ai.model               어떤 모델
ai.cost_usd            비용
ai.input_tokens        입력 토큰
ai.output_tokens       출력 토큰 (추론 토큰 포함)
ai.cache_read_tokens   캐시 읽기 토큰
ai.project             프로젝트 (안전한 basename일 때만)
ai.tools               사용한 도구 목록
ai.subscription_covered 구독으로 커버되는 호출인지
git.in_main            그 작업이 실제로 main에 머지됐는지  ← 핵심!
git.was_reverted       되돌려졌는지                        ← 핵심!
git.commit_count       커밋 수
codeburn.device_id     기기 식별자 (hostname+username의 SHA-256 앞 16자)
```

> **의미:** 가장 어려운 일(41개 툴 파싱 + 가격 계산 + 수집 클라이언트)은 이미 완성돼 있고,
> **"받아서 보여주는 서버"만 만들면 되며, 규격서까지 공개돼 있습니다.**

---

### 아이디어 1 — 팀 AI 비용 대시보드 SaaS ⭐ 최우선 추천

**고객:** CTO / 개발팀장
**그들의 고민:** "우리 팀 AI 비용이 월 2천만원인데 누가 뭘 하는지 모르겠다. 돈값을 하는지도 모르겠다."

**킬러 기능:** `git.in_main` / `git.was_reverted` 데이터로
**"쓴 돈이 실제로 제품에 반영됐는지"** 를 자동 계산할 수 있습니다.

> "AI에 2천만원 썼는데, 1,400만원어치는 실제 배포됐고, 400만원은 되돌려졌고, 200만원은 날아갔습니다."

이런 숫자를 낼 수 있는 도구는 시장에 거의 없습니다. 이게 차별점입니다.

**가격 모델 (가정):**

| 플랜 | 대상 | 가격 |
|---|---|---|
| Free | 3명까지 | ₩0 (도입용) |
| Team | ~30명 | 1인당 월 ₩9,900 |
| Business | 30명+ | 1인당 월 ₩19,900 + SSO |
| Enterprise | 대기업 | 연간 계약 + 온프레미스 |

**만들어야 할 것 (MVP):** OIDC 로그인 서버(Auth0/Cognito) → 디스커버리 JSON 응답 →
`POST /v1/traces` 수신·저장 → React 대시보드
**난이도:** ⭐⭐☆☆☆ (규격서가 있어서 생각보다 쉬움) · **예상 기간:** 혼자 4~6주

---

### 아이디어 2 — AI 비용 진단 컨설팅 💼 가장 빨리 수익

개발이 거의 필요 없습니다.

```
고객사에 codeburn optimize 실행 요청 → JSON 결과 수령
→ 분석 리포트 작성 → 개선안 제안 → 수익
```

| 상품 | 가격(가정) |
|---|---|
| 1회 진단 (리포트 + 개선안 + 발표) | 200~500만원 |
| 월간 모니터링 (리포트 + 코칭) | 월 50~150만원 |
| 팀 교육 워크숍 | 회당 100~300만원 |

**장점:** 개발 기간 0일 · 실제 고객과 대화하며 아이디어 1의 요구사항 학습 ·
컨설팅 고객이 나중에 SaaS 첫 고객이 됨
**팁:** 첫 3곳은 무료로 진행하고 후기와 사례를 확보

---

### 아이디어 3 — 슬랙/디스코드 봇 🤖

매일 아침 팀 채널에 자동 리포트:

```
🔥 어제의 AI 리포트
총 지출: ₩482,000 (전일 대비 +12%)
🥇 효율왕: 김개발  ₩30,000 / 커밋 8개
💡 팁: Opus로 오타 수정하는 패턴이 3번 감지됐어요
```

봇 자체는 무료로 배포해 사용자를 모으고, 히스토리 30일 이상·상세 분석·예산 알림을 유료화.
슬랙봇은 회사 안에 자연스럽게 침투해서 나중에 팀 단위 결제로 이어집니다.

---

### 아이디어 4 — 한국형 포크

| CodeBurn의 빈 자리 | 우리가 채우기 |
|---|---|
| 전부 영어 | 완전 한국어화 |
| 달러 기준 | 원화 + 부가세 계산 |
| 국내 툴 미지원 | 국내 AI 코딩 툴 추가 |
| 법인 서류 없음 | 세금계산서 / 경비 처리용 리포트 |

---

### 아이디어 5 — 모바일 앱 / 위젯

CodeBurn은 데스크톱만 지원합니다. 홈 화면 위젯 + 실시간 푸시 알림은 완전히 빈 자리입니다.
단, 서버가 필요하므로 아이디어 1과 묶어서 진행하는 게 좋습니다.

---

### 아이디어 6 — 콘텐츠 / 교육

`codeburn optimize` 가 찾아주는 **14가지 낭비 패턴**이 그대로 교재가 됩니다.
유튜브 → 블로그 → 전자책 / 강의 → 유료 커뮤니티. 밑천 0원으로 시작 가능.

---

### 90일 실행 플랜

```
1~30일  검증
  ├ CodeBurn을 내 데이터로 직접 사용
  ├ 개발자 커뮤니티에 수요 확인
  └ 아는 회사 2~3곳 무료 진단 (아이디어 2)

31~60일  미끼 만들기
  ├ 슬랙봇 MVP 개발 (아이디어 3)
  ├ 무료 배포 + 커뮤니티 홍보
  └ 사용 데이터 수집

61~90일  제품화
  ├ sync 수신 서버 구축 (아이디어 1)
  ├ 대시보드 개발
  └ 무료 진단해준 회사에 첫 계약 제안
```

**전략 요지:** 돈 안 드는 것(컨설팅)으로 고객을 먼저 확보하고, 그 고객의 요구사항으로 제품을 만든다.
제품 먼저 만들고 고객을 찾는 순서는 실패 확률이 높습니다.

---

### ⚠️ 반드시 알아야 할 리스크 4가지

#### ① 원본 팀이 먼저 만들 수 있다 (최대 리스크)
`sync` 는 **프리뷰** 표시가 붙어 있고, README에 **기업 스폰서 모집** 문구가 있습니다.
AgentSeal이 직접 유료 서버를 낼 가능성이 낮지 않습니다.
→ **대응:** 정면승부 대신 **한국 시장 특화**로 차별화 (한국어, 원화, 국내 법규, 국내 영업, 세금계산서)

#### ② 프로토콜이 바뀔 수 있다
문서에 *"이 기능은 프리뷰이며 릴리즈 사이에 규격이 바뀔 수 있음"* 이 명시돼 있습니다.
→ **대응:** 지원 버전 고정 + 변경 추적 필수

#### ③ 개인정보 / 근로자 모니터링 이슈 (한국에서 특히 중요)
"누가 얼마 썼는지"는 근로자 모니터링에 해당할 수 있습니다.
- 개인 단위가 아닌 **팀 단위 집계를 기본값**으로
- 직원 **동의 절차 필수** (개인정보보호법)
- 프롬프트/코드는 **절대 수집 금지** (CodeBurn도 보내지 않음)
- "감시 도구"가 아니라 **"비용 최적화 도구"** 로 포지셔닝

#### ④ 라이선스
MIT라서 상업적 이용은 가능하지만:
- 저작권 표시 유지 필수
- "CodeBurn" 이름은 그대로 쓰지 말고 별도 브랜드명 사용
- 원본 프로젝트 스폰서는 관계와 신뢰 측면에서 권장

---

### 아이디어 비교표

| 아이디어 | 난이도 | 수익까지 | 잠재력 |
|---|---|---|---|
| 컨설팅 | ⭐☆☆☆☆ | 1~2주 | 💰💰 |
| 슬랙봇 | ⭐⭐☆☆☆ | 2~3개월 | 💰💰 |
| **팀 SaaS** | ⭐⭐⭐☆☆ | 4~6개월 | **💰💰💰💰💰** |
| 한국형 포크 | ⭐⭐⭐☆☆ | 6개월+ | 💰💰💰 |
| 모바일 | ⭐⭐⭐⭐☆ | 6개월+ | 💰💰 |
| 콘텐츠 | ⭐☆☆☆☆ | 3개월+ | 💰 |

> **결론:** CodeBurn은 "측정"까지만 합니다.
> 돈은 그 다음 — 팀 관리, 정산, 알림, 리포트, 컨설팅에 있습니다.
> 그리고 **`sync` 수신 서버가 비어 있다는 점이 가장 명확한 기회**입니다.

---

## 9. React나 PHP로 만들 수 있나

### ⚛️ React — 이미 쓰고 있습니다

| 위치 | 용도 |
|---|---|
| `dash/` | 웹 대시보드 (React + Vite) |
| `windows/src/` | 윈도우 트레이 앱 UI (React) |
| `src/dashboard.tsx` | **터미널 대시보드도 React** (Ink 라이브러리) |

`package.json` 에 `react: ^19.2.5` 가 명시돼 있습니다.
→ **React로 만드는 것은 100% 가능하며, 이미 증명돼 있습니다.**

### 🐘 PHP — 가능하지만 용도를 나눠야 합니다

**PHP가 잘하는 것:** JSON 읽기, SQLite 읽기, 웹 대시보드 (PHP의 주특기)

**PHP가 불리한 것:**

| 문제 | 이유 |
|---|---|
| 배포 난이도 | Node는 `npx codeburn` 한 줄. PHP는 런타임 + 웹서버 설치 필요 |
| 터미널 UI | Ink 같은 라이브러리가 없음 |
| 대용량 파싱 속도 | 수만 개 세션 파일 처리에서 Node가 유리 |
| MCP SDK | 공식 지원이 TypeScript/Python 중심 |
| 데스크톱 앱 | 메뉴바/트레이 앱 제작 불가 |

### 추천 조합

**개인용 도구를 만든다면:** React + TypeScript(Node.js) — CodeBurn과 같은 길, `npx` 한 줄 배포

**팀 웹 대시보드(수익화 아이디어 1)를 만든다면:** PHP가 잘 맞습니다

```
[개발자 PC]                        [우리 서버]
codeburn sync push                  PHP (Laravel 등)
       │  OTLP/HTTP JSON       →    ├ 데이터 저장
       ▼                            ├ 대시보드 (React)
   (CodeBurn이 알아서 보냄)          └ 슬랙 알림
```

**핵심:** 로그 파싱은 CodeBurn에 맡기고, 우리는 그 JSON만 받아서 처리합니다.
→ 41개 툴 파싱(가장 어려운 부분)을 공짜로 얻고, PHP는 잘하는 일(웹·DB·대시보드)만 합니다.
바퀴를 다시 발명하지 말고 **CodeBurn을 부품으로 쓰는 전략**입니다.

---

## 10. 분석 중 발견한 것들

### ① `sync` 수신 서버 부재 → 사업 기회
8장 참고. 클라이언트와 규격서는 있는데 서버가 없습니다.

### ② `BRIEF.md` — 커밋되면 안 되는 파일이 커밋돼 있음
루트의 `BRIEF.md` 는 프로젝트 문서가 아니라 **AI 에이전트에게 준 작업 지시서**입니다.
내용은 이슈 #1143 수정 지시 (백그라운드 인덱싱 중 `q` 를 누르면 약 16.5초간 조용히 멈추는 문제 개선)이며,
**지시서 안에 "BRIEF.md는 커밋하지 말 것" 이라고 적혀 있는데 실제로는 커밋돼 있습니다.**
기능에는 영향이 없고, 원본 저장소에서 딸려온 것으로 보입니다. 정리하려면 삭제하면 됩니다.

### ③ 터미널 UI에 React 사용
`src/dashboard.tsx` 에서 **Ink** 라이브러리로 터미널 화면을 React 컴포넌트로 렌더링합니다.

### ④ 플러그인 로더의 보안 설계
`src/plugins/loader.ts` 는 **deny-by-default** 입니다.
매니페스트 오류, 이름/디렉터리 불일치, CLI 버전 범위 이탈, 서명 검증 실패 중
하나라도 있으면 로딩을 거부하고 그 이유를 그대로 출력합니다.

---

## 부록 — 자주 쓰는 환경변수

| 변수 | 설명 |
|---|---|
| `CLAUDE_CONFIG_DIR` | Claude Code 데이터 디렉터리 (기본 `~/.claude`) |
| `CLAUDE_CONFIG_DIRS` | 여러 Claude 디렉터리를 함께 스캔 (POSIX `:`, Windows `;`) |
| `CODEX_HOME` | Codex 데이터 디렉터리 (기본 `~/.codex`) |
| `AI_GATEWAY_API_KEY` | Vercel AI Gateway 연동용 (유일하게 진짜 API 키가 필요한 곳) |
| `CODEBURN_PLUGIN_DEV=1` | 서명되지 않은 플러그인 로딩 허용 (개발용) |

전체 목록은 원본 README의 **Environment Variables** 섹션을 참고하세요.

---

*이 문서는 저장소 코드와 문서를 직접 열람해 작성했습니다.*
*가격·수익 관련 숫자는 모두 예시이자 가정이며, 실제 시장 검증이 필요합니다.*
